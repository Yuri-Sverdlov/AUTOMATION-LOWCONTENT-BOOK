# CONTEXT.md — Память проекта

> Читать **первым делом**. Медленный слой: суть проекта, стек, роли, текущий фокус.
> История сессий — в `PROJECT_LOG.md`. Активное задание — в `tasks/TASK.md`.

**Обновлено:** 2026-06-11

---

## Суть проекта

`AUTOMATION-LOWCONTENT-BOOK` — конвейер для производства **low-content книг** под
Amazon KDP (линованные блокноты, журналы, планировщики и т.п.). Цель: кодом готовить
готовые к загрузке файлы (interior.pdf + cover.png + crib.md), оставив человеку только
ручную загрузку в KDP. Полная стратегия — в `PROJECT-OVERVIEW.md`, структура данных —
в `STRUCTURE.md`.

**Красная линия:** никакой автоматизации браузера против KDP (бан). См. `AGENTS.md`.

---

## Разделение ролей

- **Архитектор** (чат, дорогая модель) — стратегия, ТЗ, проверка, логи.
- **Кодер** (терминал, дешёвая модель, напр. DeepSeek/Qwen через Claude Code CLI) —
  пишет код по `tasks/TASK.md`, отчитывается в `tasks/REPORT.md`.

Обмен — только через файлы. Каноническая файловая система описана в `AGENTS.md`.

---

## Стек (предварительно)

- **Интерьер PDF:** Python + `reportlab` (генерация по геометрии KDP).
- **Обложка:** `Pillow` (позже).
- **LLM (названия/описания):** Claude API или Ollama; есть `reusable/python/llm_client.py`.
- **Данные разведки:** Publisher Rocket / Book Bolt, Amazon autocomplete, Google Trends.

---

## Эталон: Серия 1 (уже выпущено вручную)

В `reference/series-1/` лежат файлы реально выпущенной книги (сделана в Book Bolt):

- `interior_reference.pdf` — 110 страниц, **7.5 × 9.25", No Bleed, ч/б, белая бумага**.
- `sample_page_05.json` — образец линованной страницы (Book Bolt формат).
- `sample_page_01.json` — образец страницы 1 (декоративная картинка, не линовка).
- `KDP_SETTINGS.txt` — настройки KDP для этой книги.

**Структура книги (контракт):** стр. 1 — декоративная картинка; стр. 2–110 —
одинаковые **wide-ruled, 26 линий**. Точные числа — в `tasks/TASK.md`.

---

## Текущий фокус

Физика книги собрана и принята:
- **`engine/interior_lined.py`** (reportlab) — линованный интерьер, копия серии 1.
- **`engine/cover_generator.py`** (Pillow) — параметрический разворот KDP (bleed,
  корешок от числа страниц, safe-зоны, proof-картинка).

`data/niches/2026-06_test/output/2026-06-05/book-1/` = реальные `interior.pdf` + `cover.png`
от наших движков. book-2/3 — заглушки.

**book-1 готов к ручной загрузке в KDP:** `interior.pdf` + `cover.pdf` + `crib.md`.

### Гибридная обложка — все 4 кирпича готовы
1. **Арт** (ComfyUI/RunPod, внешний) — чистый фон без текста.
2. **Раскладка** — vision-модель (или архитектор вручную, «шаг А») выдаёт JSON: где/как
   ставить текст. Тест-кейс: `data/niches/2026-06_test/sources/layout_test.json`.
3. **Текст кодом** — `engine/text_layout.py`: рисует текст по JSON (авто-кегль/перенос/
   контраст/scrim). Тест-арт: `sources/images/front_art_test.png`.
4. **Разворот+PDF** — `engine/cover_generator.py` (front_image) + `engine/cover_to_pdf.py`.

**Следующее — «сборка» в один проход:** art + layout JSON → text_layout → вставить
готовый фронт как `front_image` в cover_generator (полный разворот) → cover_to_pdf.
Затем: автоматизация шага А через vision-API; мост ComfyUI→папка ниши; данные ниши
в `niche.yaml`; сборщик `crib.md` из паспорта; пакетная генерация серии.

---

## «Зона безопасности» наложения текста — ПРИНЯТО (2026-06-08)

Тест на реальной обложке (`tasks/1.png` арт без текста, `tasks/2.png` эталон) показал:
слепые архетипы A–E наезжают на занятый угол (halftone-сфера), т.к. движок про арт не знает.
Решение принято и реализовано: зрение (архитектор, «шаг А») задаёт `safe_box` + потолок кегля
ОДИН раз, дальше детерминированный веер A–E работает ВНУТРИ очищенной зоны. Ноль коллизий.

- `text_layout.py` — `max_font_pct` в спецификации → `max_font_size` в `_autofit_font`.
- `layout_variants.py` — `generate_variants(..., safe_box_pct=[x,y,w,h])`; `_build_spec`
  пересчитывает боксы архетипов относительно зоны. `safe_box_pct=None` → геометрия как раньше.

Подход SAFE признан пользователем правильным. Приёмка — `tasks/done/2026-06-08_safe_box/`.
Демо-сравнение: `tasks/lv_test_safe/full|safe/_gallery.png` (тест-папки, в git НЕ идут).

**git-чекпоинт принят** (`tasks/done/2026-06-08_git-checkpoint/ACCEPTED.md`): движки на GitHub.

**ПРИНЯТО (2026-06-08):** цепочка `engine/build_cover.py` (оркестратор) + фронт-fill в
`cover_generator` — передний PNG → полный разворот (фронт cover-fit до bleed, DPI-защита,
задник плоский) → `cover.pdf`+`cover.png` в `data/niches/<ниша>/output/<дата>/book-N/`.
Приёмка: `tasks/done/2026-06-08_build-cover-chain/ACCEPTED.md`. Тест-ниша `2026-06_snarky-notebook`.
Передний арт пока низкого разрешения (заглушка) — мягкий; крепкость даст реальный арт ≥300 DPI.

**Урок:** shell-песочница может показывать устаревший снимок репозитория — при расхождении
доверять прямым файловым инструментам (live-файл), не выводам sandbox.

**ПОВОРОТ ПОДХОДА (2026-06-08):** пользователь решил рисовать **полный разворот сам** в
редакторе (пиксель-в-пиксель под Amazon). Скрипт упрощается до «валидатор размера + обёртка
в PDF»: `config.yaml` (размер разворота в in/cm + путь) → сверка с `размер×300` (±1 px) →
совпало: `cover.png`+`cover.pdf` в дерево ниши; не совпало: `raise` с требуемым размером.
cover-fit/DPI-турникет ОТМЕНЕНЫ для этого потока; `cover_generator` остаётся для будущего
авто-пайплайна, но в простом режиме не используется.

**ПРИНЯТО (2026-06-08):** простой режим `wrap_validated_cover` в `build_cover.py` + `config.yaml`
(in/cm). Сверяет размер разворота (±1 px) → совпало: `cover.png`+`cover.pdf` в дерево ниши;
не совпало: `raise` с требуемым размером. `build_cover_chain` (авто-сборка) сохранён, но из
CLI не вызывается. Приёмка: `tasks/done/2026-06-08_simple-validate-wrap/ACCEPTED.md`.

---

## Ревизия проекта (2026-06-11, сессия 10) — выводы и приоритеты

Полная ревизия свежей моделью (детали — PROJECT_LOG, сессия 10). Сильные стороны:
процессная дисциплина (петля TASK/REPORT, объективные приёмки) и замкнутая «физика
книги». **Главный перекос:** ~90% усилий в Фазе 2 (производство), ~0% в Фазах 1/4
(разведка, обратная связь); конвейер ещё не выпустил ни одной реальной книги.

**Принятый порядок работ:**

1. **«Дешёвая четвёрка»** (быстрые задачи с высокой отдачей, бэклог):
   crib-сборщик из паспорта · параметризация interior от `niche.yaml`
   (сейчас движок паспорт НЕ читает — константы) · pytest-смоук геометрии
   (закрывает и ручную приёмку, и хронические глюки mount-снимков) ·
   дубль-чек `published.csv` (анти-бан, обещан стратегией, не реализован).
2. **Первая реальная книга через конвейер** — Фаза 1 вручную, реальный `niche.yaml`,
   выпуск в KDP, строка в `published.csv`, трекинг BSR
   (`tasks/backlog/2026-06_first-real-book.md`). Стратегический приоритет.
3. Дальше прежний бэклог: vision-автоматизация шага А, палитры, задняя обложка и т.д.

**Статус движков:** `interior_lined.py`, `build_cover.py` (simple-режим),
`cover_to_pdf.py` — боевые. `cover_generator.py` (auto), `text_layout.py`,
`layout_variants.py` — **спящие** после поворота s8 (сохранены для будущего
авто-пайплайна, не на боевом пути). Известный хвост спящего кода:
`cover_proof.png` при `draw_guides=True` в AUTO-режиме не появлялся (s9).

**Мелкий мусор:** lock-файлы `~$OJECT-OVERVIEW.md`, `~$RUCTURE.md` в корне —
удалить с диска (в git не идут, в `.gitignore` есть).

---

**Конвейер названий — этапы 1–2 готовы и приняты (сессия 11).** Дизайн закрыт
(`tasks/backlog/2026-06_title-pipeline.md`, раздел «Принятые решения» + памятка z-score).
Боевые модули: `engine/titles/generator.py` (один генератор) и `engine/titles/pool.py`
(3 шутника × 1..N наборов крючков → структурированный пул кандидатов с `id`/`author_role`/
`hook_set_index`). Персоны `prompts/joker_1..3.md` (юмор/эмпатия/минимализм). Конфиг
`config/models.yaml` (режим `advanced`, Вариант А: одна модель `minimax/minimax-M3`,
температуры 0.8/0.9/1.0; арбитры 0.25 — заготовка). Тесты `tests/test_title_parse.py`,
`tests/test_pool.py`. Живой smoke на OpenRouter подтвердил работу (12 кандидатов).
Архивы: `tasks/done/2026-06-11_titles-brick1/`, `tasks/done/2026-06-11_titles-stage2/`.

**Следующий этап (3):** арбитры — промпты из рубрики DOE (`reference`:
`G:\AI\_MY_PROGRAMMING_4\DOE-ORIGINAL-TITLE\execution\prompts\judge.md`), round-robin
(арбитр-N не судит шутника-N), 6 критериев с весами, жёсткие фильтры (safety<7, длина KDP),
свёртка сумма/z-score → ранжированный список. Промптов арбитров пока НЕТ — это задача этапа 3.

**Полный пайплайн спроектирован (сессия 13).** Дизайн зафиксирован в
`MY-COMMENTS/pipeline-design.md` + SVG-карты (01 — обзор, 02 — шаги 3–4–5).
Ключевые решения: SEO-ядро → только в шаг 5 (Title+Blurb), НЕ в названия;
судья шага 5 оценивает пару Title+Blurb как единое целое (критерий coherence);
21 блокнот/неделю = 7 × 3 (лимит KDP). Backlog: `2026-06_pipeline-flowchart.md`
(блок-схема с ветвлениями).

**Сейчас в `tasks/TASK.md`:** git-чекпоинт s13 (РАЗРЕШЕНО коммитить) —
всё накопленное с `49a1ea4`: `MY-COMMENTS/`, 8 папок `tasks/done/`, backlog-файл,
обновлённые CONTEXT/PROJECT_LOG, TASK/REPORT. После пуша — снова «не коммитить».
Примечание для сверки: `tests/` покрывают только titles — геометрия книги без тестов,
backlog `2026-06_pytest-smoke.md` актуален.

**⚠ Безопасность:** в сессии 11 реальный ключ OpenRouter случайно попал в `.env.example`/
`REPORT.md`, устранён (ключ только в `.env`, он в `.gitignore`); **ключ рекомендовано перевыпустить**.

**Бэклог (`tasks/backlog/`):**
- **Ревизия s10 (приоритет):** `2026-06_crib-builder.md`,
  `2026-06_parametrize-interior.md`, `2026-06_pytest-smoke.md`,
  `2026-06_published-csv-dedup.md`, `2026-06_first-real-book.md`.
- `2026-06_art-palette-automation.md` — генерация фронтов, отличающихся ТОЛЬКО палитрой
  (фон + 3–4 перекрашиваемых слоя, 10–12 палитр на вход).
- `2026-06_params-config-ui.md` — ввод параметров через конфиг-файл и/или браузерный UI.
- задняя обложка (плоский фон + чистая зона под штрихкод KDP) — оформить отдельным TASK.
- `2026-06_size-helper-and-templates.md` — калькулятор «trim+страницы→размер разворота в px»
  и набор PNG-болванок под стандартные размеры Amazon (слои-гайды для подгонки арта).
- `2026-06_title-pipeline.md` (s11) — конвейер генерации НАЗВАНИЙ-крючков: 3 генератора ×
  6–7 вариантов → 3 арбитра (round-robin) ставят 1–10 по рубрике → индекс (сумма / z-score) →
  ранжируемый список. Рубрика из 6 критериев + эталон `DOE-ORIGINAL-TITLE`. Этапы 1–2 приняты.

**Дальше:** автоматизация шага А (layout JSON) через vision-API, мост ComfyUI→ниша,
сборщик `crib.md` из паспорта, пакетная генерация серии.
