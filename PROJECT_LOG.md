# PROJECT_LOG.md

Append-only журнал сессий проекта `AUTOMATION-LOWCONTENT-BOOK`.
Новые записи добавляются сверху. Пишет архитектор в конце сессии.

---

## 2026-06-11 — Сессия 11: конвейер названий (этапы 1–2) + закрыт дизайн

**Сделано:**

- **Закрыт дизайн** `tasks/backlog/2026-06_title-pipeline.md`: 7 открытых вопросов →
  принятые решения (Вариант А сейчас / B на будущее; все настройки агента — в конфиге;
  разнонаправленные температуры; оба шлагбаума `safety_risk<7` и длина KDP + `audience_fit`
  из `niche.yaml`; свёртка веса+z-score, дефолт z-score; наборы крючков 1..N). Добавлена
  памятка по z-score (формула, пример, аналогия).
- **Каркас конфига:** `config/models.yaml` + `models.example.yaml` (режим `advanced`,
  joker_1/2/3 t=0.8/0.9/1.0, arbiter_1/2/3 t=0.25, провайдер openrouter), `.env.example`.
- **Этап 1 принят** (`tasks/done/2026-06-11_titles-brick1/`): `engine/titles/generator.py`
  (load_niche / build_generation_prompt / parse_titles / generate_titles) + `prompts/joker_1.md`
  + `tests/test_title_parse.py`. Архитектор проверил живьём (import, dry-run, 6 тестов).
- **Этап 2 принят** (`tasks/done/2026-06-11_titles-stage2/`): `prompts/joker_2.md` (эмпатия),
  `joker_3.md` (минимализм), `sources/hooks.example.yaml` (рыба), `engine/titles/pool.py`
  (`build_pool` с инъекцией `generator_fn`, пул кандидатов с `id`/`author_role`/`hook_set_index`),
  `tests/test_pool.py` (6/6). **Живой smoke на OpenRouter сработал:** модель `minimax/minimax-M3`
  валидна, 12 кандидатов. `requirements.txt` пополнен (openai, python-dotenv, pyyaml).
- **⚠ Инцидент безопасности:** реальный ключ OpenRouter попал в `.env.example` и `REPORT.md`
  (оба под git). Устранено: ключ перенесён в `.env` (в `.gitignore`), example→плейсхолдер,
  в REPORT замаскирован. Пользователю рекомендован **перевыпуск ключа**.
- **Урок (подтверждён снова):** bash-песочница показала устаревший `requirements.txt` (3 пакета
  вместо 6) — при приёмке сверять live-файлом. Поэтому коммит делаем на реальной Windows-машине.
- Новый `tasks/TASK.md`: git-чекпоинт сессии 11 (коммит+пуш РАЗРЕШЁН). REPORT сброшен.

**Дальше:** этап 3 — арбитры (промпты из рубрики DOE), round-robin, 6 критериев,
жёсткие фильтры, свёртка сумма/z-score → ранжированный список.

---

## 2026-06-11 — Сессия 10: внешняя ревизия проекта (свежий взгляд модели)

**Сделано:**

- **Принят чекпоинт s9** по REPORT (push `084b9cf`, stash дропнут, дерево clean) →
  архив `tasks/done/2026-06-10_git-checkpoint-s9/` (+ACCEPTED.md).
- **Проведена полная ревизия проекта** свежей моделью: доки, движки (1293 строки),
  журнал 9 сессий, бэклог, ниши. Оценка: процессная дисциплина и «физика книги» —
  на отлично; **главный перекос — ~90% усилий в Фазе 2 (производство), ~0% в
  Фазах 1/4 (разведка, обратная связь)**. Конвейер ещё не выпустил ни одной реальной
  книги: `niche.yaml` — TODO, `published.csv` — пустой заголовок.
- **Замечания ревизии:** (1) движки НЕ читают `niche.yaml` — «параметрический движок»
  пока декларация; (2) crib-сборщик откладывается с сессии 2; (3) дубль-чек
  `published.csv` (анти-бан) не реализован; (4) нет автотестов — при хронических
  глюках mount-снимков pytest на хосте закрыл бы и регрессии, и доверие к приёмке;
  (5) `cover_generator`/`text_layout`/`layout_variants` — спящий код после поворота
  s8, статус помечен в CONTEXT; (6) мусор `~$*.md` в корне — удалить с диска.
- **Бэклог пополнен (5 файлов):** `2026-06_crib-builder.md`,
  `2026-06_parametrize-interior.md`, `2026-06_pytest-smoke.md`,
  `2026-06_published-csv-dedup.md`, `2026-06_first-real-book.md` (стратегический).
- **Принят порядок работ** (зафиксирован в CONTEXT): «дешёвая четвёрка» → первая
  реальная книга через конвейер (Фаза 1 вручную) → прежний бэклог (vision шаг А,
  палитры, задник). Обсуждение приоритетов с пользователем — отложено, по его слову.
- Новый `tasks/TASK.md`: git-чекпоинт ревизии s10 (коммитить РАЗРЕШЕНО), REPORT.md
  сброшен в ожидание.
- **Принят чекпоинт s10:** проверено объективно (git show --stat на live-репо) —
  коммит `bbdf614` = ровно 12 ожидаемых файлов, мусор `~$*` удалён, push прошёл.
  Архив → `tasks/done/2026-06-11_git-checkpoint-s10/`. Замечание кодера: 2 backlog-файла
  (`art-palette-STAGES`, `external-ideas-research`) не были в списке TASK (недогляд
  архитектора) — остались untracked. Решение: микро-чекпоинт, новый `tasks/TASK.md`.

---

## 2026-06-10 — Сессия 9 (этот ПК): sync + перенос проектов + приёмка сборщика

**Сделано:**

- **Синхронизация с GitHub:** `git pull --ff-only` подтянул работу с другого ПК
  (`68cb7f2` → `69cf603`): `engine/build_cover.py` (сборщик), `config.yaml`, ниша
  `2026-06_snarky-notebook`, backlog (4 задачи). Локальные правки убраны в stash.
  В `AGENTS.md` вручную возвращена ссылка на `G:\_My_Programming\DEV-NOTES.md`.
- **Перенос 4 книжных проектов** `G:\_My_Programming\` → `G:\_MY_PROGRAMMING_4\`
  (переименование в ЗАГЛАВНЫЕ-через-дефис): DOE-ORIGINAL-TITLE, DOE-CONTENT-AUTOMATION,
  DB-WRITING-SYSTEM, ANTIGRAVITY-STORY-ORBITA. Robocopy с проверкой (счётчики совпали,
  Failed=0), источники удалены. Стале-пути в доках перечислены — починить позже.
- **Принят сборщик `engine/build_cover.py`** (auto + simple режимы): все 5 шагов
  верификации зелёные (разворот 4649×2850, PDF 15.4967×9.5 in, валидация размера +
  понятный ValueError). Архив → `tasks/done/2026-06-10_verify_build_cover/`.

**Открытые хвосты:** (1) `cover_proof.png` при draw_guides в AUTO — перепроверить;
(2) git stash от sync — дропнуть; (3) стале-пути в доках DOE; (4) терминал на Sonnet 4.6.

**Заметка по среде:** mount-копии песочницы периодически усекаются (ловили на
`text_layout.py`, `cover_generator.py`, при `cp` — на `TASK.md`). Архитектор не может
надёжно прогонять код локально; приёмка — по host-Read + отчёту кодера. См. DEV-NOTES.

---

## 2026-06-08 — Сессия 8: цепочка обложки → простой режим валидации (принято)

**Сделано (3 этапа, все приняты, проверка — живыми файловыми инструментами):**

- **git-чекпоинт** «зоны безопасности» подтверждён (коммиты `2017568`, `171e121` уже были
  на `origin/main`). → `tasks/done/2026-06-08_git-checkpoint/`.
- **`engine/build_cover.py`** (оркестратор) + правка `cover_generator` (фронт **cover-fit**
  до bleed + DPI-warning). r0 собрал мелкий фронт по центру → r1 исправлен (живой proof:
  фронт заполняет крышку). → `tasks/done/2026-06-08_build-cover-chain/`.
- **ПОВОРОТ:** пользователь решил рисовать **полный разворот сам** (пиксель-в-пиксель под
  Amazon). Скрипт упрощён до **`wrap_validated_cover`**: `config.yaml` (размер in/cm + путь)
  → сверка размера (±1 px) → совпало: `cover.png`+`cover.pdf` в дерево ниши; не совпало:
  `raise` с требуемым размером. cover-fit/DPI-порог в простом режиме не на пути,
  `cover_generator` сохранён для будущей авто-сборки. → `tasks/done/2026-06-08_simple-validate-wrap/`.

**Бэклог пополнен:** задняя обложка (зона штрихкода), палитровая автоматизация арта,
конфиг/UI параметров, калькулятор размера разворота + PNG-болванки под размеры Amazon.

**Урок (повтор сессии 6):** bash-песочница снова отдавала устаревший снимок (обрезанный
`cover_generator.py`, старые output-файлы) → ложная тревога при приёмке. Сверять live-файлами;
записано в память.

**Дальше:** git-чекпоинт сессии 8 (этот коммит). Затем — калькулятор размера разворота
(дешёвый, полезен сразу) либо задняя обложка.

---

## 2026-06-08 — Сессия 7: «зона безопасности» наложения текста (принято)

**Сделано:**

- Тест наложения на **реальной обложке** пользователя (`tasks/1.png` арт без текста,
  `tasks/2.png` эталон «MY BRAIN'S WEIRDEST HITS»). Прогнали спор «5 слепых вариантов
  vs шаг А»: слепые архетипы A/C наезжают на halftone-сферу — движок про арт не знает.
  Вывод: перебор и шаг А — не конкуренты, а стадии; узкое место обоих — второй раунд.
- **Принят** этап «зона безопасности»: зрение задаёт `safe_box` + потолок кегля ОДИН раз,
  дальше детерминированный веер A–E работает внутри очищенной зоны.
  - `text_layout.py`: `max_font_pct` → `max_font_size` в `_autofit_font` (потолок кегля).
  - `layout_variants.py`: `generate_variants(..., safe_box_pct=[x,y,w,h])`; `_build_spec`
    пересчитывает боксы относительно зоны. `safe_box_pct=None` → геометрия бит-в-бит как раньше.
  - Объективная проверка (REPORT.md, 5/5): на `1.png` зона `[4,40,70,56]` — все 5 bbox внутри,
    кегль 42–53px (был 74–81), текст чист от сферы. Задание → `tasks/done/2026-06-08_safe_box/`.
- Демо-сравнение: `tasks/lv_test_safe/full|safe/` (тест-папки, в git не идут).

**Дальше:** в `tasks/TASK.md` — git-чекпоинт (закоммитить движки). После пуша — новая сессия,
возврат к «сборке в один проход» гибридной обложки.

---

## 2026-06-06 — Сессия 6: приёмка layout_variants.py + чекпоинт git

**Сделано:**

- **Принят** `engine/layout_variants.py` (+доработка `text_layout.py`: leading/tracking).
  Галерея `output/layout_variants/_gallery.png` показывает 5 заведомо разных раскладок
  по архетипам (A center-hero / B bottom-band+scrim / C centered-airy / D top-left /
  E lower-center). Исходник на диске полный и корректный, метрики отчёта консистентны.
  Реализует идею «генерация вариантов → отбор» (дёшево, без GPU). Задание →
  `tasks/done/2026-06-06_layout_variants/`.
- ⚠️ **Глюк песочницы архитектора:** mount-копия `engine/text_layout.py` зависла
  обрезанной (236 стр.) → повторный прогон в песочнице падал ложно. Реальный файл
  (host) полный (287 стр.). НЕ баг кодера. Вывод на будущее: если bash-проверка
  внезапно противоречит Read — сверять `wc -l`/`inspect.getsource` (host vs mount).

**Накоплено много незакоммиченного** (с момента клона 58c8477 — ничего не коммичено).
Следующий шаг: чекпоинт-задание кодеру — `git add/commit/push` всего на GitHub
(коммит делается на машине пользователя, где git работает; в `.gitignore` добавлены
`~$*` и `.claude/`).

---

## 2026-06-06 — Сессия 5: приёмка text_layout.py (текст на арт по JSON)

**Сделано:**

- **Принят** `engine/text_layout.py` — рисователь текста на арт по JSON-спеку.
  Проверено объективно (запуск на тест-кейсе ниши + просмотр PNG): заголовок белым
  лёг в заданную рамку, авто-перенос + авто-подбор кегля (DejaVu 66px/5 строк в Linux,
  Arial 70px/4 строки на Windows — движок подстраивается), авто-контраст, scrim и
  align работают. Все 8 ✅. Задание → `tasks/done/2026-06-06_text_layout/`.
- Тест-кейс гибридной обложки в нише: `sources/images/front_art_test.png` +
  `sources/layout_test.json` → `output/text_layout_test.png` (фактически готовая
  передняя обложка с текстом).

**Все 4 кирпича гибридной обложки готовы:** арт (ComfyUI, внешний) → раскладка
(vision-JSON, шаг А) → текст кодом (`text_layout.py`) → разворот+PDF
(`cover_generator.py` + `cover_to_pdf.py`).

**Дальше:** «сборка» — связать кирпичи в один проход: art + layout JSON →
text_layout → вставить как `front_image` в cover_generator (полный разворот) →
cover_to_pdf. Плюс: автоматизация шага А через vision-API, мост ComfyUI→ниша.

---

## 2026-06-06 — Сессия 4: приёмка cover_to_pdf.py + book-1 готов к загрузке

**Сделано:**

- **Принят** PNG→PDF: кодер написал `engine/cover_to_pdf.py`. Архитектор проверил
  объективно (pypdf + рендер poppler): одностраничный PDF, MediaBox **1115.76×684 pt**
  (±0.24 от эталона 1116×684), углы рендера = фон обложки (нет белых полей),
  эффективный DPI 300, пропорции сохранены, 200 стр. → 1130.4 pt (шире). Все 7 ✅.
  Задание → `tasks/done/2026-06-05_cover_to_pdf/`.
- **book-1 полностью готов к ручной загрузке в KDP:** `interior.pdf` + `cover.pdf`
  + `crib.md`. `cover.png` остаётся дизайн-мастером (в KDP не грузится).

**Веха:** сквозной путь «код → готовые к KDP файлы одной книги» замкнут. Дальше —
не новая физика, а масштабирование/наполнение: данные ниши, типографика обложки,
сборка crib из паспорта, пакетная генерация серии.

**Открытый пункт:** `crib.md` book-1 не дописан (файл был открыт/заблокирован) —
обновить строку «что грузить»: interior.pdf + cover.pdf в KDP, cover.png не грузить.
Возможно стоит дополнить `STRUCTURE.md` (добавить `cover.pdf` рядом с `cover.png`).

---

## 2026-06-05 — Сессия 3: приёмка cover_generator.py (Pillow)

**Сделано:**

- **Принят** генератор обложки: кодер написал `engine/cover_generator.py` (Pillow).
  Архитектор проверил объективно (запуск + Pillow + просмотр proof): разворот
  **4649×2850** (±1 от эталона 4650×2850), RGB, корешок **74 px** по центру,
  зоны по формулам (front_trim_right = full_w−bleed = 4611 ✓), safe-зоны соблюдены,
  proof с гайдами + чистый финал. Параметризация: 60 стр.→41px, 200→135px (по формуле).
  DPI в PNG = 299.9994 — целочисленное хранение формата, не баг (KDP смотрит на пиксели).
  Все 8 критериев ✅. Задание → `tasks/done/2026-06-05_cover_generator/`.
- **book-1 замкнут на реальные файлы:** `cover.png` теперь генерится нашим движком
  (был плейсхолдер). Теперь book-1 = реальный interior.pdf + реальный cover.png.

**Физика книги собрана:** интерьер (reportlab) + обложка-разворот (Pillow) — обе
половины, где KDP чаще всего отклоняет, воспроизводятся кодом параметрически.

**Дальше (кандидаты):** (а) обёртка обложки PNG→PDF для загрузки в KDP-print;
(б) `niche.yaml` → реальные данные ниши (разведка); (в) сборщик `crib.md` из паспорта;
(г) `front_image`/типографика обложки под палитру паспорта.

---

## 2026-06-05 — Сессия 2: приёмка interior_lined.py + дерево data/

**Сделано:**

- **Принят** первый срез: кодер написал `engine/interior_lined.py` (reportlab) +
  `requirements.txt`. Архитектор проверил объективно (pypdf + сверка PNG): 110 стр.,
  MediaBox 540×666, 26 линий/стр., X 28.35→521.10, первая 641.7 pt, шаг 24.696,
  цвет #999999, толщина 0.48, стр.1 пустая. Все 8 критериев ✅. Геометрия = эталон.
- Задание заархивировано в `tasks/done/2026-06-05_interior_lined/` (+ACCEPTED.md).
- Развёрнуто дерево `data/` по `STRUCTURE.md`: `shared/`, `published.csv` (заголовок),
  ниша `2026-06_test/` с `niche.yaml` (реальная геометрия + TODO-маркетинг).
- День `2026-06-05`: **book-1 — реальный `interior.pdf`** + `crib.md` + плейсхолдер
  `cover.png`; book-2/3 — заглушки (демонстрируют ритм 3/день).

**Дальше (кандидаты):** генератор обложки на Pillow (заполнит реальные cover.png);
паспорт ниши `niche.yaml` → реальные данные после разведки; сборщик `crib.md` из паспорта.

---

## 2026-06-05 — Сессия 1: развёртывание и канонический комплект файлов

**Сделано:**

- Развёрнут репозиторий `AUTOMATION-LOWCONTENT-BOOK` в рабочую папку.
- Изучены стратегия (`PROJECT-OVERVIEW.md`), структура (`STRUCTURE.md`) и
  код `reusable/`. Вывод: стратегия зрелая, ядро продукта (генератор интерьера,
  обложки, паспорт ниши, шпаргалка, журнал) ещё не написано.
- Изучены образцы серии 1. Извлечены точные спеки линованного интерьера
  (7.5×9.25, wide-ruled, 26 линий, шаг 24.696 pt, цвет #999999). Скопированы
  эталоны в `reference/series-1/`.
- Выбрана модель для кодера: **DeepSeek V4** (Anthropic-совместимый эндпойнт для
  Claude Code, отдельный биллинг — не трогает лимит Pro). Альтернативы: Qwen3 Coder
  (дешевле, через OpenRouter), локальный Ollama (бесплатно).
- Собран **канонический комплект** файловой системы архитектор↔кодер:
  `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, `tasks/TASK.md`, `tasks/REPORT.md`,
  `tasks/done/`, `PROJECT_LOG.md`.
- Поставлено первое задание: `tasks/TASK.md` → `engine/interior_lined.py`
  (строгая копия линованного интерьера серии 1).

**Дальше:** кодер выполняет `tasks/TASK.md`, заполняет `REPORT.md`; архитектор
проверяет PDF против эталона.
