# BACKLOG / ДИЗАЙН: конвейер генерации названий-крючков (титулов)

**Создано:** 2026-06-08 (архитектор) · **Статус:** дизайн обсуждён, ТЗ дорабатываем в новой сессии.
**Это главный итог сессии 8 (вторая половина) — конструктор названий, ещё не кодили.**

---

## Цель пользователя (его слова)

Запустить алгоритм и получить «на склад» ~**12 названий книг на ближайшие 4 дня**.
Вход: пользователь РУКАМИ выбирает на Amazon 2 фразы-крючка из топа (легально, без скрейпинга)
и передаёт их генераторам.

## Архитектура воронки (5 ступеней пользователя)

1. **Генерация:** 3 агента («Шутник-1/2/3»), каждый на входе 2 крючка → выдаёт **6–7 вариантов**
   названий. Итого ~18–21 кандидат.
2. **Отбор/оценка:** те же 3 агента, но с другими системными промптами выступают как
   **«Арбитр-1/2/3»**.
3. **Список:** все кандидаты сводятся в один список.
4. **Отбор по критериям:** жёсткие фильтры (safety, длина KDP, ≤7 ключей).
5. **Баллы:** каждый арбитр ставит 1–10 по критериям; **индекс = сумма баллов арбитров**;
   список ранжируется по индексу.

## Два варианта реализации (можно МИКСОВАТЬ по ступеням)

- **V1 — одна мультимодальная LLM** (напр. opus 4.6) пишет 3 разных системных промпта-стиля
  → 3 «личности». Дёшево, стабильно, но голос модели один → варианты коррелируют.
- **V2 — три разные модели** (через API или локально). Настоящее разнообразие, дороже/сложнее.
- **Решение пока:** на ГЕНЕРАЦИИ важно разнообразие → ближе к V2 (или явно разные стили +
  высокая температура); на ОЦЕНКЕ важнее единая шкала → можно одна модель в роли 3 арбитров.

## Антиперекос судейства

- **Round-robin против самооценки:** Арбитр-N НЕ оценивает варианты своего же автора (Шутник-N).
  Тогда каждый кандидат судят ровно **2 арбитра** (делитель = 2, а не 3) — честно и сравнимо.
- **Две схемы свёртки баллов (реализовать ОБЕ, чтобы сравнить ранжирование):**
  - **A. Сырая сумма** — просто складываем все баллы.
  - **B. z-score** — у каждого арбитра `z=(балл−его среднее)/его разброс`, потом сумма z.
    Убирает перекос «строгий vs добрый судья». Нужно ≥6–7 кандидатов на судью (у нас есть).
    Делитель дисперсии (n или n−1) выбрать один и применять ко всем судьям.

## Критерии арбитра — ГОТОВАЯ рубрика из прошлого проекта (взять за основу)

Источник: `G:\AI\_MY_PROGRAMMING_4\DOE-ORIGINAL-TITLE\execution\prompts\judge.md` (Stage 1).
Каждый критерий 1–10; веса — из `DOE-ORIGINAL-TITLE\data\config.yaml`:

| Критерий | Вес | Смысл |
|---|---|---|
| hook_strength | 0.30 | насколько цепляет внимание |
| clarity | 0.20 | понятность с первого прочтения |
| youth_culture_fit | 0.15 | попадание в аудиторию (там 20–45) |
| originality | 0.15 | новизна, мало клише |
| cover_brevity | 0.10 | лаконичность под обложку |
| safety_risk | 0.10 | безопасность формулировки (политика/маркетплейс) |

Механика прошлого проекта: **взвешенная** сумma (`Σ балл×вес`), НЕ простая; жёсткий отсев
`safety_risk < 7 → выброс`; тай-брейк по hook_strength; в финал не берут почти-дубликаты.
Там было: **1 судья** (sonnet), 3 РАЗНЫЕ модели-генератора (haiku/opus/qwen3:14b), 6 вариантов/агент.
Идея пользователя (3 арбитра + z-score) — развитие этого.

## Открытые вопросы (решить в новой сессии, до ТЗ)

1. **Веса vs равный вклад** критериев. Можно совместить z-score + веса.
2. **safety_risk hard-gate (<7 → выброс)** — оставляем? (рекомендую да).
3. **youth_culture_fit под нишу** — переопределять из `niche.yaml` (аудитория), а не фиксировать 20–45.
4. **Длина названия KDP** — добавить отдельный жёсткий критерий (символьный лимит).
5. **Сколько пар крючков** кормит один прогон (одна пара → однообразный финал; возможно несколько).
6. **Модели:** что на генерации, что на оценке; V1/V2 по ступеням.
7. **Режим свёртки по умолчанию** — сырая сумма или z-score (реализовать оба).

## Кирпичи (что уже есть в проекте)

- `reusable/python/llm_client.py` ⭐⭐⭐ — мотор: один `.call()` к OpenAI/Anthropic/Gemini/
  DeepSeek/OpenRouter/Ollama. Нужен `config/models.yaml` + `.env`. Это сердце генерации.
- `reusable/python/title_blurb_orchestrator.py` ⭐⭐ — паттерн «A/B/C → судья» + устойчивый
  разбор JSON из ответа LLM. Привязан к чужому проекту → переносим ПАТТЕРН, не дословно.
- `reusable/python/agents.py` ⭐ — скелет Generator→Judge→Refiner + рубрика баллов (контент — заглушка).
- `reusable/js/title-generator.js` ⭐⭐ — офлайн-шаблонный генератор (образец «шаблон+словари+перемешивание»).
- `reusable/js/data-reader.js` ⭐⭐⭐ — валидация (обязательные поля, ≤7 ключей, цена) → ступень «фильтр».

## Эталон-референс (рабочий прошлый конвейер)

`G:\AI\_MY_PROGRAMMING_4\DOE-ORIGINAL-TITLE` — полный двухстадийный пайплайн:
- Stage 1: генераторы A/B/C → судья → top-6 названий. `execution/orchestrator.py`
  (взвешенный счёт, dedup, `normalize_and_rank_top6`), `execution/prompts/{generators,judge}.md`.
- Stage 2: пара «product_title + blurb» → судья выбирает 1. `execution/stage2/title_blurb_orchestrator.py`,
  `execution/prompts/title_blurb_judge.md`.
- Конфиг (веса, модели): `data/config.yaml`.
> Доступ к этой папке выдан пользователем в сессии 8; в новой сессии может потребоваться
> заново подключить её (`request_cowork_directory`).

## Первый кирпич для ТЗ (черновик)

Минимальная ступень: подключить `llm_client.py`, прочитать `niche.yaml`, сгенерировать N
названий по 2 крючкам одним агентом → JSON-список. Затем достроить 3 генератора, 3 арбитра
(round-robin), рубрику и две схемы свёртки (сумма / z-score).
