# REPORT.md — Отчёт кодера

> Заполняет **кодер** после выполнения `tasks/TASK.md`. Один активный отчёт.
> Будь честен: что не получилось — важнее, чем что получилось.

**Задание:** `engine/interior_lined.py` — строгая копия линованного интерьера серии 1
**Дата:** 2026-06-05
**Статус:** `готово`

---

## 1. Что сделано

- Создан `engine/interior_lined.py` — генератор PDF-интерьера (reportlab, чистая геометрия).
- Создан `requirements.txt` с зависимостями `reportlab` и `pypdf`.
- Создана папка `engine/` (новая).
- Сгенерирован `output/interior_lined_test.pdf` (110 стр., 7.5×9.25", No Bleed, wide-ruled 26 линий).
- Сгенерированы PNG для визуальной сверки: `output/check_gen_page2.png`, `output/check_ref_page2.png`.

## 2. Как запускать

```bash
# Установить зависимости (если не установлены)
pip install -r requirements.txt

# Сгенерировать тестовый PDF (110 стр., дефолты серии 1)
python engine/interior_lined.py
# Результат: output/interior_lined_test.pdf

# Из кода:
# from engine.interior_lined import build_lined_interior
# build_lined_interior("output/my.pdf", pages=110)
```

## 3. Результаты проверок (критериев приёмки)

| # | Критерий | Результат | Фактические числа |
|---|----------|-----------|-------------------|
| 1 | MediaBox = `0 0 540 666` на всех страницах | ✅ | 540.0 × 666.0 на всех 110 стр. |
| 2 | Число страниц = 110 | ✅ | 110 |
| 3 | Число линий на стр. 2 = 26 | ✅ | 26 moveto + 26 lineto |
| 4 | Геометрия линий (стр. 2): Y_top первой = 24.30 pt, шаг = 24.696 pt, X_left = 28.35, X_right = 521.10 | ✅ | Y_top=24.3000, шаг=24.6960, X_left=28.35, X_right=521.10 |
| 5 | Цвет = #999999, толщина 0.48 pt | ✅ | цвет #999999, толщина 0.48 |
| 6 | Стр. 1 пустая (0 линий) | ✅ | 0 линий, front_matter_image=None |
| 7 | PDF открывается без ошибок, число стр. читается | ✅ | открыт pypdf без exception |
| 8 | Визуальная сверка стр. 2 ген. vs эталон | ✅ | см. ниже |

**Визуальная сверка (критерий 8):**
Обе страницы отрендерены через pymupdf (2× zoom) в PNG (`output/check_gen_page2.png` vs `output/check_ref_page2.png`). Линовка визуально совпадает: 26 серых горизонтальных линий, одинаковый шаг, одинаковые поля слева и справа. Единственное отличие — рендер Book Bolt в эталоне чуть более контрастный (возможно, другой рендерер); геометрия идентична.

## 4. Что НЕ получилось / отклонения

- Нет отклонений от ТЗ.
- Декоративная картинка стр. 1 — заглушка (TODO-комментарий в коде), как и требовалось.
- Кириллица в print() вызвала UnicodeEncodeError в cp1252-терминале — заменён текст на ASCII `"Done: ..."` (на логику не влияет).

## 5. Вопросы архитектору

- Нет вопросов. Готов к следующей задаче.
