# TASK: конвейер названий — кирпич 1 (один генератор → JSON-список)

**От:** архитектор → **кому:** кодер (терминал)
**Дата:** 2026-06-11
**Тип:** новая разработка (старт подсистемы названий)
**Git:** ⛔ коммитить/пушить НЕ нужно. И НИКОГДА не клади реальные API-ключи в файлы под git.

---

## Контекст

Стартуем конвейер генерации названий-крючков. Полный дизайн и принятые решения —
в `tasks/backlog/2026-06_title-pipeline.md` (прочитай раздел «Принятые решения» и
«Приложение: z-score»). Сейчас делаем **минимальный первый кирпич**: один генератор
(«шутник») берёт набор крючков + паспорт ниши и выдаёт JSON-список из N вариантов названий.
Арбитры, round-robin, свёртка баллов — В ЭТО ЗАДАНИЕ НЕ ВХОДЯТ (следующие кирпичи).

**Мотор уже есть:** `reusable/python/llm_client.py` — класс `LLMClient(role, config_path)`,
метод `.call(system_prompt, user_message) -> str`. Читает `config/models.yaml`.
Образец устойчивого разбора JSON из ответа LLM — `reusable/python/title_blurb_orchestrator.py`
(перенеси ПАТТЕРН разбора, не копируй дословно).

**Красная линия:** крючки приходят АРГУМЕНТОМ от человека (он выбрал их на Amazon руками).
Никакого скрейпинга/автоподбора крючков из Amazon в коде.

---

## Что нужно сделать

### 1. Конфиг моделей — УЖЕ СОЗДАН архитектором (НЕ пересоздавать)

Каркас конфига уже на месте, пересоздавать не нужно — только использовать:
- `config/models.yaml` — режим **`advanced`**, роли `joker_1/2/3` (температура 0.8/0.9/1.0)
  и `arbiter_1/2/3` (0.25), `provider: openrouter`, `api_key_env: OPENROUTER_API_KEY`,
  `model` = плейсхолдер `"TODO-..."` (реальную строку впишет Юрий);
- `config/models.example.yaml` — образец-копия;
- `.env.example` — образец (`OPENROUTER_API_KEY=`); `.env` уже в `.gitignore` (строка 6).

Важно про `advanced`: `llm_client` возвращает `advanced[role]` как есть (строка ~86),
поэтому каждый ролевой словарь самодостаточен (provider/model/temperature/max_tokens/
api_key_env). Если для кирпича 1 чего-то не хватает — допиши в конфиг, но структуру и роли
сохрани. **Реальный `.env` с ключом НЕ создавай и НЕ коммить.**

### 2. Модуль генератора — `engine/titles/generator.py`

Создать пакет `engine/titles/` (с `__init__.py`) и модуль `generator.py` со следующими
функциями (разделение на чистые функции и вызов API — обязательно, чтобы тестировать офлайн):

- `load_niche(niche_path) -> dict` — прочитать `niche.yaml`, вернуть нужные поля:
  `niche`, `audience`, `interior_type`, `interior_style`, `format`, `title_formula`.
- `build_generation_prompt(persona_system: str, hooks: list[str], niche: dict, n: int) -> tuple[str, str]`
  — ЧИСТАЯ функция: возвращает `(system_prompt, user_message)`. `system_prompt` = персона
  (перед