# ПРИЁМКА: конвейер названий — этап 1 (один генератор → JSON)

**Принял:** архитектор · **Дата:** 2026-06-11 · **Вердикт:** ✅ ПРИНЯТО (офлайн-критерии)

## Проверено архитектором живьём (не по отчёту)

- `import engine.titles.generator` — без ошибок.
- dry-run на тестовой нише: в промпте видны оба крючка, `audience`, тип интерьера.
- `tests/test_title_parse.py` — все 6 тестов проходят (чистый JSON, ```json```-фенс,
  текст+JSON, мусор без падения, дедуп, обрезка пробелов). Прогнан напрямую (pytest в
  песочнице отсутствует; логика подтверждена self-testّ`ом).
- Код по ТЗ: чистые функции отделены от вызова API; `parse_titles` устойчив (3 стратегии +
  дедуп + пустой список на мусоре); при `[ERROR]` от провайдера не падает.

## Замечания (не блокеры, переходят в этап 2 / Фазу 1)

1. `audience` в `data/niches/2026-06_test/niche.yaml` — заглушка (данные Фазы 1).
2. Модель `minimax/minimax-M3` в конфиге не верифицирована живым вызовом (возможна опечатка
   slug; у MiniMax обычно вида `minimax/minimax-01`). Проверяется первым живым прогоном.
3. Живой вызов не выполнялся (песочница без сети к OpenRouter; ключ на момент ТЗ отсутствовал).
   Перенесён в этап 2 как обязательный дымовой тест.
4. Зависимости `openai`, `python-dotenv`, `pyyaml` отсутствуют в `requirements.txt` —
   добавить в этапе 2 (без `python-dotenv` ключ из `.env` не подхватывается).

## Артефакты этапа 1

`config/models.yaml`, `config/models.example.yaml`, `.env.example`,
`engine/titles/{__init__.py, generator.py}`, `engine/titles/prompts/joker_1.md`,
`tests/test_title_parse.py`.
