# ПРИЁМКА: конвейер названий — этап 2 (3 шутника + пул кандидатов)

**Принял:** архитектор · **Дата:** 2026-06-11 · **Вердикт:** ✅ ПРИНЯТО (все 7 критериев)

## Проверено архитектором живьём

- `import engine.titles.pool` — без ошибок.
- `tests/test_pool.py` — **6/6 passed** (через pytest; при прямом запуске молчит — в файле
  нет `__main__`-блока, это не дефект).
- dry-run `pool` показывает план (2 набора × 3 роли = 6 вызовов, до 36 названий) и промпт.
- `pool.py` по ТЗ: `build_pool` с инъекцией `generator_fn` (тестируемо без API), устойчив к
  ошибкам генерации, кандидат = `{id, title, author_role, hook_set_index, hooks}`.
- Персоны `joker_2.md` (эмпатия/тепло) и `joker_3.md` (минимализм/польза) — голоса заметно
  различны.
- `requirements.txt` пополнен (`openai`, `python-dotenv`, `pyyaml`) — **подтверждено live-файлом**
  (bash-снимок был устаревшим: показывал 3 пакета вместо 6 — см. урок в PROJECT_LOG s11).
- Живой smoke кодера: модель `minimax/minimax-M3` валидна, 6 вызовов → 12 кандидатов, валидный JSON.

## Замечания (не блокеры)

- `audience` в тестовой нише по-прежнему заглушка (Фаза 1).
- Кросс-дедуп кандидатов между шутниками не делается намеренно (разнообразие; забота арбитров).
- В отчёте кодера п.1 был помечен ✅, но архитектор отдельно перепроверил requirements
  по live-файлу из-за хронических устаревших снимков песочницы.

## Артефакты этапа 2

`engine/titles/pool.py`, `engine/titles/prompts/joker_2.md`, `joker_3.md`,
`data/niches/2026-06_test/sources/hooks.example.yaml`, `tests/test_pool.py`,
`requirements.txt` (+3 пакета).
