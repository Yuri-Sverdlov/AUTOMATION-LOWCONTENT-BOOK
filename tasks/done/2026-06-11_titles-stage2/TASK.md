# TASK: конвейер названий — этап 2 (3 шутника + пул кандидатов)

**От:** архитектор → **кому:** кодер (терминал)
**Дата:** 2026-06-11
**Тип:** новая разработка (достройка ступени генерации)
**Git:** ⛔ коммитить/пушить НЕ нужно. Реальные ключи в файлы под git НЕ класть.

---

## Контекст

Этап 1 принят (`tasks/done/2026-06-11_titles-brick1/`): есть один генератор
`engine/titles/generator.py` (функции `load_niche`, `build_generation_prompt`,
`parse_titles`, `generate_titles`) + персона `prompts/joker_1.md`. Дизайн и решения —
`tasks/backlog/2026-06_title-pipeline.md` (читай «Принятые решения»).

Сейчас достраиваем **ступень генерации целиком**: 3 шутника, 1..N наборов крючков на
запуск, и сборка всех вариантов в **структурированный пул кандидатов** (JSON). Этот пул —
вход для будущей ступени арбитров. **Арбитры / баллы / z-score / фильтры — НЕ в этом этапе.**

**Вариант А (как решено):** одна модель у всех шутников, разница — персона + температура
(в `config/models.yaml` у joker_1/2/3 уже t=0.8/0.9/1.0). Тебе НЕ нужно менять модели.

**Красная линия:** крючки даёт человек (выбрал на Amazon руками). Код их не добывает.
Для теста используешь «рыбу» — выдуманные крючки в файле-образце.

---

## Что нужно сделать

### 1. Зависимости

В `requirements.txt` добавить: `openai`, `python-dotenv`, `pyyaml`. Установить их
(`pip install -r requirements.txt` или `pip install <pkg> --break-system-packages`).
Без `python-dotenv` ключ из `.env` НЕ подхватывается (см. `llm_client.py`, строки 16–22) —
живой вызов вернёт «API ключ не задан».

### 2. Персоны шутников joker_2 и joker_3

Создать `engine/titles/prompts/joker_2.md` и `joker_3.md` по образцу `joker_1.md`, но с
ЯВНО разными голосами (чтобы варианты не совпадали). Рубрика/цель та же. Идея разницы
(на твоё усмотрение, но пусть будут различимы):
- joker_1 — уже есть (юмор/дерзость);
- joker_2 — например «тёплый, эмпатичный, забота о читателе»;
- joker_3 — например «минимализм, чистый бенефит, без воды».
Каждая персона по-прежнему обязана: использовать крючки, вернуть ТОЛЬКО JSON-массив строк,
без ™/®/эмодзи.

### 3. Формат входа: наборы крючков (1..N)

Поддержать НЕСКОЛЬКО наборов крючков за запуск. Формат — YAML-файл:
- создать образец `data/niches/2026-06_test/sources/hooks.example.yaml` (РЫБА):
  ```yaml
  # hooks.example.yaml — наборы крючков на вход генерации.
  # РЫБА (заглушки). Для боевого прогона скопируй в hooks.yaml и впиши реальные
  # топовые фразы с Amazon (человек выбирает руками — код их не добывает).
  hook_sets:
    - ["productivity boost", "goal crusher"]
    - ["self care journal", "anxiety relief"]
  ```
- «набор» = 