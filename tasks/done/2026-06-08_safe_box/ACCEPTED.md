Принято архитектором 2026-06-08. Все 5 критериев пройдены (проверено по REPORT.md + просмотр галерей `tasks/lv_test_safe/full|safe/_gallery.png`).

Суть: добавлена «зона безопасности» наложения текста.
- `text_layout.py` — `max_font_pct` в спецификации элемента → потолок кегля в `_autofit_font` (новый аргумент `max_font_size`). Без ключа — поведение прежнее.
- `layout_variants.py` — `generate_variants(..., safe_box_pct=[x,y,w,h])`; `_build_spec` пересчитывает боксы архетипов ОТНОСИТЕЛЬНО зоны. При `safe_box_pct=None` геометрия бит-в-бит как раньше (критерий №1: A/E совпали).

Объективная проверка на `tasks/1.png` (550×674), зона `[4,40,70,56]`:
- FULL (старое): A/C/D наезжают на сферу, кегль 74–81px.
- SAFE (новое): все 5 bbox внутри зоны, кегль 42–53px, текст чист от сферы.

Решение пользователя: подход SAFE признан правильным, автоматизация сохраняется.
