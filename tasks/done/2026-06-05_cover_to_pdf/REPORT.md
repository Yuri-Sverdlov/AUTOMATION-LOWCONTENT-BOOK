# REPORT.md — Отчёт кодера

> Заполняет **кодер** после выполнения `tasks/TASK.md`. Один активный отчёт.
> Будь честен: что не получилось — важнее, чем что получилось.

**Задание:** `engine/cover_to_pdf.py` — обёртка обложки PNG → PDF для загрузки в KDP
**Дата:** 2026-06-05
**Статус:** `готово`

---

## 1. Что сделано

- Создан `engine/cover_to_pdf.py` — функция `cover_png_to_pdf(png_path, pdf_out, dpi=300)`.
- Сгенерированы:
  - `output/cover_test.pdf` (из cover_test.png, 110 стр.)
  - `output/cover_test_200p.pdf` (из cover_test_200p.png, 200 стр.)
- Для проверки края-в-край отрендерена страница PDF в PNG через pymupdf.

---

## 2. Как запускать

```bash
# Быстро: обернуть output/cover_test.png → output/cover_test.pdf
python engine/cover_to_pdf.py

# Из кода:
# from engine.cover_to_pdf import cover_png_to_pdf
# dims = cover_png_to_pdf("output/cover.png", "output/cover.pdf")
# # dims: {w_px, h_px, dpi, page_w_pt, page_h_pt}
```

---

## 3. Результаты проверок (критериев приёмки)

| # | Критерий | Результат | Фактические числа / комментарий |
|---|----------|-----------|----------------------------------|
| 1 | Одна страница в PDF | ✅ | 1 |
| 2 | MediaBox ≈ 1116×684 pt (±1 pt) для cover_test.png | ✅ | **1115.76 × 684.00 pt** (diff_w=0.24, diff_h=0.00) |
| 3 | Картинка заполняет страницу край-в-край (нет полей) | ✅ | Рендер PDF → PNG (pymupdf, 0.3×); угловые пиксели all corners = **#f2ece3** (фон обложки, не белый) |
| 4 | Эффективное разрешение ≈ 300 DPI (±1) | ✅ | **300.0000 DPI** (w и h) |
| 5 | PDF валиден, pypdf читает без exception | ✅ | `len(pages)=1`, ошибок нет |
| 6 | pages=200 → PDF шире по MediaBox (оба значения) | ✅ | 110 стр.: **1115.76×684.00 pt**; 200 стр.: **1130.40×684.00 pt** (шире на 14.64 pt) |
| 7 | Соотношение сторон сохранено (нет растяжения) | ✅ | ratio_px=1.631228, ratio_pt=1.631228, diff=0.00000000 |

---

## 4. Что НЕ получилось / отклонения

- Нет отклонений от ТЗ.
- MediaBox по ширине = 1115.76 pt (а не ровно 1116), разница 0.24 pt — в пределах допуска ±1 pt. Причина: 4649 px / 300 × 72 = 1115.76 (формула даёт дробь; целое было бы только при DPI = 4649/72·300 ≈ нецелое).

---

## 5. Вопросы архитектору

- Нет вопросов. Готов к следующей задаче.
