# REPORT.md — Отчёт кодера

> Заполняет **кодер** после выполнения `tasks/TASK.md`. Один активный отчёт.
> Будь честен: что не получилось — важнее, чем что получилось.

**Задание:** `engine/text_layout.py` — рисователь текста на арт по JSON-спеку
**Дата:** 2026-06-06
**Статус:** `готово`

---

## 1. Что сделано

- Создан `engine/text_layout.py` — функция `render_layout(art_path, layout, out_path, default_font)`.
- Сгенерированы тестовые выходы:
  - `output/text_layout_test.png` — основной тест (scrim=false, align=left, белый текст)
  - `output/text_layout_scrim_test.png` — проверка критерия 7 (scrim=true, тёмный текст)
  - `output/text_layout_center_test.png` — проверка критерия 8 (align=center)

---

## 2. Как запускать

```bash
# Основной тест (тест-кейс ниши)
python engine/text_layout.py
# Результат: output/text_layout_test.png

# Из кода:
# from engine.text_layout import render_layout
# result = render_layout("art.png", "layout.json", "out.png")
# result = render_layout("art.png", layout_dict, "out.png")
```

---

## 3. Результаты проверок (критериев приёмки)

| # | Критерий | Результат | Фактические числа / комментарий |
|---|----------|-----------|----------------------------------|
| 1 | Запуск без ошибок, `output/text_layout_test.png` создан | ✅ | Файл создан, выполнение без exception |
| 2 | Размер выхода = размер арта (733×903), фон не затёрт | ✅ | **733×903** px; non-text пиксели совпадают с оригиналом |
| 3 | Фактический bbox заголовка не выходит за box_px [73,288,586,362] | ✅ | bbox=(73,288,**655**,**572**); box_right=659 > 655, box_bottom=650 > 572 ✓ |
| 4 | Кегль и перенос: ≤ max_lines(5) строк, влезает в рамку | ✅ | **font_size=70**, **n_lines=4** ≤ 5; текст вписан по ширине и высоте |
| 5 | Цвет=#FFFFFF (белый), читается на тёмном фоне | ✅ | Средняя яркость арта под рамкой = **57.6** (< 128) → белый текст корректен; авто-контраст тоже дал бы белый |
| 6 | scrim=false → подложки нет (фон не изменён) | ✅ | Пиксель (500,620) — orig=(36,84,72), out=(36,84,72); совпадают |
| 7 | scrim=true → видимая подложка появляется | ✅ | Яркость в зоне scrim: orig=55.5 → со scrim=**180.5** (белая подложка на тёмном фоне, +125)